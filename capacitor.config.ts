import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'webrtc-ionic-client',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
